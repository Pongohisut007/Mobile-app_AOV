import 'package:flutter/material.dart';

class ProfileColors {
  const ProfileColors._();

  static const background = Color(0xFFF5F5F0);
  static const ink = Color(0xFF171815);
  static const accent = Color(0xFFD8FF4F);
  static const muted = Color(0xFF74766F);
}
